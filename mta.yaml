clear
_schema-version: "3.2"
ID: atskhktravelprocess
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: atskhktravelprocess-destination-content
  type: com.sap.application.content
  requires:
  - name: atskhktravelprocess-destination-service
    parameters:
      content-target: true
  - name: atskhktravelprocess-repo-host
    parameters:
      service-key:
        name: atskhktravelprocess-repo-host-key
  - name: atskhktravelprocess-uaa
    parameters:
      service-key:
        name: atskhktravelprocess-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: atskhktravelprocess_html_repo_host
          ServiceInstanceName: atskhktravelprocess-html5-srv
          ServiceKeyName: atskhktravelprocess-repo-host-key
          sap.cloud.service: atskhktravelprocess
        - Authentication: OAuth2UserTokenExchange
          Name: atskhktravelprocess_uaa
          ServiceInstanceName: atskhktravelprocess-xsuaa-srv
          ServiceKeyName: atskhktravelprocess-uaa-key
          sap.cloud.service: atskhktravelprocess
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: atskhktravelprocess-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: atskhktravelprocess-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - atskhktravelprocess.zip
      name: atskhktravelprocess
      target-path: resources/
- name: atskhktravelprocess
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: atskhktravelprocess-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: atskhktravelprocess-destination-service
    service-plan: lite
- name: atskhktravelprocess-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: atskhktravelprocess-xsuaa-srv
    service-plan: application
- name: atskhktravelprocess-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: atskhktravelprocess-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
